Select-String -Path "C:\logs\pass_run\template_summary.json" -Pattern '"statistics"' -Context 0,5

findstr /C:"\"statistics\"" C:\logs\pass_run\template_summary.json




Теперь нам нужно посмотреть какие именно 106 шаблонов появились в FAIL и какие 3 есть в PASS.
В PowerShell выполни:

$pass = Get-Content ".\PASS_RUN\template_summary.json" -Raw | ConvertFrom-Json
$fail = Get-Content ".\FAIL\template_summary.json" -Raw | ConvertFrom-Json

Потом:

$pass.templates | Select-Object count, template

Это покажет все 3 PASS-шаблона.
После этого:

$fail.templates |
    Sort-Object count -Descending |
    Select-Object -First 30 count, template

Это покажет 30 самых часто встречающихся шаблонов FAIL.
И ещё одна очень полезная команда:

$passTemplates = $pass.templates.template

$fail.templates |
    Where-Object { $_.template -notin $passTemplates } |
    Sort-Object count -Descending |
    Select-Object -First 30 count, template

Она покажет:
Какие шаблоны присутствуют в FAIL, но вообще отсутствуют в PASS.


Что делаем сейчас
Ничего в template_extractor.py пока не меняй.
Теперь выполни эту команду для PASS:

$pass.templates |
    Sort-Object count -Descending |
    Select-Object count, template

У тебя PASS-шаблонов всего 3, поэтому она покажет их все.
Мне нужно увидеть эти 3 строки.
После этого выполни ещё для FAIL:

$fail.templates |
    Sort-Object count -Descending |
    Select-Object -First 15 count, template

Но второй результат у нас частично уже есть на фотографии, поэтому главное сейчас — покажи 3 шаблона PASS.
Почему это важно: у PASS всего 3 unique_templates, а у FAIL 106. Это очень большая разница:



python .\compare_templates.py `
".\PASS_RUN\template_summary.json" `
".\FAIL\template_summary.json"

##№₽################₽₽₽$######₽


. В PowerShell выполни:

Select-String -Path ".\FAIL\run_comparison_report.txt" `
-Pattern "FILES PRESENT ONLY IN PASS" -Context 0,30

и:

Select-String -Path ".\FAIL\run_comparison_report.txt" `
-Pattern "FILES PRESENT ONLY IN FAIL" -Context 0,30

Посмотри, есть ли там много файлов.
Ещё выполни:

Get-Content ".\FAIL\run_comparison_report.txt" | Select-String "TEST:"

Если TEST: вообще ничего не показывает, значит file_comparisons действительно пустой.


###########################

Сначала проверь синтаксис:
python3 -m py_compile drain_test.py

Потом я бы не запускал сразу на всей огромной PASS-папке. Возьми тот же небольшой набор логов, на котором мы уже тестировали наши шаблоны:

python3 drain_test.py /path/to/TEST_FOLDER

После завершения появится:

drain_templates.json

Посмотреть первые самые частые шаблоны можно так:

python3 -c "import json; d=json.load(open('drain_templates.json')); [print(x['count'], x['template']) for x in d['templates'][:30]]"

Нас интересует, сможет ли Drain3 самостоятельно сделать хорошие шаблоны для твоих реальных случаев, например свести многочисленные: